interface:
  display_name: "Geki Spec Review"
  short_description: "Run bounded risk-based specification reviews"
  default_prompt: "Use $geki-spec-review to validate the current checkpoint, record stable findings, and run at most a baseline and delta closure unless critical risks remain."
